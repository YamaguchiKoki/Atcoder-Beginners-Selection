#include<bits/stdc++.h>
using namespace std;   //std省略のため

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    string s;
    cin >> s;
    cout << (a + b + c) << " " <<  s << endl;
    return 0;
}
